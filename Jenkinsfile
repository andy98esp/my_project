pipeline {
    agent push

    stages {
        stage('Print Workspace') {
            steps {
                script {
                    echo "Workspace: ${workspace}"
                }
            }
        }

        // ... other stages of your pipeline ...
    }
}
